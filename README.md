# video-generation-front-end-user

Static user-facing page.

- Params: `?language=English&word=apple` (language is currently forced to `English`; word defaults to `apple`)
- Behavior: looks up an existing video for `(word, language)`, otherwise queues generation and shows “In generation — coming later.”
